# ============================================================================
# Ansible Inventory File
# ============================================================================
# This file tells Ansible where to run the playbook
# For local setup, use the [local] section below
# ============================================================================

[local]
localhost ansible_connection=local

# Example: Uncomment and modify below if you want to run on remote servers
# [remote_servers]
# server1 ansible_host=your-server-ip ansible_user=ubuntu
# server2 ansible_host=another-server-ip ansible_user=ubuntu
