---
- hosts: all
  vars_files:
    - /home/inventory/host_vars/localhost.yaml
  roles:

    - role: debug
      tags: debug

    - role: common
      tags:
        - devops

    - role: browsers
      tags:
        - web

    - role: terminal-shell-configuration

    - role: communication
